version: "3.8"

services:
  web:
    image: waterloo-guesser:latest
    container_name: waterloo-guesser-web
    restart: always
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
