name: Deploy Server to EC2

on:
  push:
    branches: [main]
    paths:
      - "server/**"
      - "server.mts"
      - "package.json"
      - "package-lock.json"
      - "Dockerfile"
      - "docker-compose.prod.yml"
      - ".github/workflows/deploy-websocket.yml"
  workflow_dispatch: # Allow manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm test

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to EC2
        run: |
          # Define target directory
          TARGET_DIR="/home/ec2-user/waterloo-guesser-app"

          # Create directory on server
          ssh -i ~/.ssh/id_rsa ec2-user@${{ secrets.EC2_HOST }} "mkdir -p $TARGET_DIR"

          # Sync files to EC2
          # Using rsync avoids git issues and ensures exact state match
          rsync -avz -e "ssh -i ~/.ssh/id_rsa" \
            --exclude '.git' \
            --exclude '.github' \
            --exclude 'node_modules' \
            --exclude '.next' \
            --exclude 'photos' \
            ./ ec2-user@${{ secrets.EC2_HOST }}:$TARGET_DIR/
            
          # Execute deployment on server
          ssh -i ~/.ssh/id_rsa ec2-user@${{ secrets.EC2_HOST }} "bash -s" << 'EOF'
          set -e
          cd /home/ec2-user/waterloo-guesser-app

          echo "🚀 Starting deployment..."

          echo "🔄 Rebuilding and restarting containers..."
          docker-compose -f docker-compose.prod.yml down
          docker-compose -f docker-compose.prod.yml up -d --build

          echo "⏳ Waiting for containers to start..."
          sleep 20

          echo "🔍 Checking container status..."
          docker-compose -f docker-compose.prod.yml ps

          echo "📊 Checking health endpoint..."
          # Check internal health first
          if curl -f http://localhost:3000/health || curl -f http://localhost:3001/health; then
            echo "✅ Internal health check passed."
          else
            echo "⚠️ Internal health check failed or timed out. Checking public endpoint..."
          fi

          # Check public endpoint
          if curl -f https://ws.uwguesser.com/health; then
            echo "✅ Deployment successful! Public health check passed."
          else
            echo "❌ Public health check failed!"
            docker-compose -f docker-compose.prod.yml logs --tail=50
            exit 1
          fi
          EOF

      - name: Notify success
        if: success()
        run: |
          echo "🎉 Server deployed successfully!"
          echo "🌐 Check https://ws.uwguesser.com/health"

      - name: Notify failure
        if: failure()
        run: |
          echo "💥 Deployment failed! Check the logs above."
